(define-shorthand "ls" "ls --color")

(define (fmt string . args)
  (format #f string args))

(set! scheme-switch #\§)

(define-history-rule (lambda (txt) #t))

(define-builtin "cd.."
  (lambda (lst)
    (chdir "..")
    (cdr lst)))

(define ($ string)
  "splices in the resut of running "
  (let* ((port (open-input-pipe string))
  	 (strs (get-string-all port)))
    (close-port port)
    strs))


  
    
  